layout(rgba8ui, binding = SAT_INPUT_IMAGE_BINDING) restrict uniform uimage2D img_in;
layout(rgba32ui, binding = SAT_OUTPUT_IMAGE_BINDING) restrict uniform uimage2D img_out;

layout(
    local_size_x = SAT_WORKGROUP_SIZE_X,
    local_size_y = SAT_WORKGROUP_SIZE_Y) in;

void main()
{
}